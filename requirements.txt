# ExactTranscriber v2.0 Requirements
# Python 3.11+ required

# Core dependencies
streamlit>=1.38.0
pydantic-ai>=1.67.0,<2
pydantic>=2.5.0
google-genai>=1.67.0,<2

# Audio processing
pydub==0.25.1
# Python 3.13+ compatibility shim for pydub's removed stdlib audioop dependency
audioop-lts>=0.2.2; python_version >= "3.13"
python-multipart>=0.0.6
aiofiles>=23.2.1
nest-asyncio>=1.5.6

# Data processing
protobuf>=4.25.0

# ================================================================
# OPTIONAL: Parakeet support for candidate generation and alignment
# ================================================================
# These packages are NOT required for Gemini-only transcription.
# Install separately if you want Parakeet as a transcript candidate
# or for post-judge timestamp alignment:
#
#   pip install nemo_toolkit[asr] torch torchaudio
#
# Requirements:
# - ~3GB disk space for models
# - CUDA GPU recommended (works on CPU but slower)
#
# The app will gracefully skip Parakeet features if NeMo is not
# installed.
# ================================================================
